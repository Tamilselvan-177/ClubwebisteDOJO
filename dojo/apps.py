from django.apps import AppConfig


class DojoConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'dojo'
    verbose_name = 'Sentinels Dojo (CTF integration)'
