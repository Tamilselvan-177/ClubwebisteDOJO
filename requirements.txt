# ============================================
# Cyber Sentinels Website - Core dependencies
# Django + CTF Dojo. Works on Windows & Linux (VPS).
# Removed: Flask, Wagtail, Jupyter, pywin32, pywinpty, opencv, matplotlib, etc.
# ============================================
# Optional (uncomment if needed):
#   psycopg2-binary==2.9.11   # PostgreSQL
#   django-filter==25.2       # DRF filtering
# ============================================
markdown
Django==4.2
python-decouple==3.8
django-jazzmin==3.0.0
djangorestframework==3.14.0
PyJWT==2.8.0
django-cors-headers==4.3.1
daphne==4.0.0
channels==4.0.0
channels-redis==4.2.0
redis==5.0.1
celery==5.3.4
django-celery-beat==2.5.0
django-celery-results==2.5.1
whitenoise==6.6.0
gunicorn==21.2.0
Pillow==11.0.0
